# Local development (non-Docker)
DATABASE_URL=postgresql://username:password@localhost:5432/vulnbank
JWT_SECRET_KEY=replace-with-a-long-random-secret-key

# Docker Compose overrides (optional — defaults are development-only placeholders)
# POSTGRES_USER=vulnbank
# POSTGRES_PASSWORD=vulnbank-dev-password
# POSTGRES_DB=vulnbank
# JWT_SECRET_KEY=docker-dev-jwt-secret-not-for-production-use
